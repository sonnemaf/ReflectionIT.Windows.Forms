namespace ReflectionIT.Windows.Forms {
    /// <summary>
    /// Action enum for a DualList
    /// </summary>
    public enum DualListAction {
        MoveSelected = 0,
        CopySelected = 1,
        MoveAll = 2,
        CopyAll = 3,
    }
}
